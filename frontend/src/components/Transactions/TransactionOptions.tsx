import * as React from 'react';

export interface ITransactionOptionsProps {
    transactionId: string
}

export default function TransactionOptions (props: ITransactionOptionsProps) {

    // Update and delete transaction components will go here

  return (
    <div>

    </div>
  );
}
